seed: 42
samples: 1500
train_split: 0.8
batch_size: 16
epochs: 10
lr: 0.0003
hidden_dim: 32
input_dim: 80
num_blocks: 4
concepts_per_block: 3
mode: baseline
output_dir: outputs
device: auto

task:
  type: multiclass
  num_classes: 4

dataset:
  type: minif2f_like
  path: data/minif2f_prepared.jsonl
  train_path: data/minif2f_prepared_train.jsonl
  val_path: data/minif2f_prepared_val.jsonl
  test_path: data/minif2f_prepared_test.jsonl

wandb:
  enabled: false
  project: oars-colab-mvp
  run_name: null
